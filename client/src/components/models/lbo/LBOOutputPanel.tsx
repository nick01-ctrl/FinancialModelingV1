export default function LBOOutputPanel() {
  return (
    <div className="flex items-center justify-center h-full text-gray-400 text-sm">
      LBO output panel — coming in next build
    </div>
  );
}
